export default function ExpenseList(props){
    const icon=props.icon;
    const amount=props.amount;
    console.log(amount);
    const note=props.note;

    return (
        <div className="rounded-sm bg-[color:var(--primary-btn)] mt-2 mb-2 min-h-[60px] h-[60px] w-[90%]">
            
        </div>
    )
}